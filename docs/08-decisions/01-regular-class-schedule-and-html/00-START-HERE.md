# 01. 정규 클래스 폼 — 일정 유형과 HTML 상세 업로드

> **상태: 완료 (2026-08-03).** 마이그레이션 적용·코드·DB 검증까지 끝났다.
> 남은 것은 **어드민 UI에서 사람이 직접 저장해 보는 것**과, 결정기록 02가
> [release gate](../02-education-programs-public-pages/00-START-HERE.md)를 닫는 것뿐이다.
> 작업 기록: [docs/07-dev/10-작업로그-2026-08-03.md](../../07-dev/10-작업로그-2026-08-03.md)

정규 클래스 등록/수정 폼에 **강의 일정**과 **HTML 상세 본문**을 추가한다.

> **2026-08-04: 업로드 상한이 512KB → 5MB로 올라갔다.** 이 폴더 문서에 적힌
> 512KB는 전부 옛 값이다. 같은 값이 4곳(`html-upload.tsx` · `actions.ts` ·
> `next.config.ts`의 `bodySizeLimit` · 동반 테이블 CHECK)에 있고,
> 마이그레이션은 `20260804120000_html_raw_size_limit.sql`이다.

> **여기서 받는 HTML은 `.html` 파일 한 개뿐이다.** 이미지·서브 페이지가 딸린
> 여러 파일 묶음은 텍스트 컬럼 하나로 담을 수 없어서
> [05-course-bundle-storage](../05-course-bundle-storage/00-START-HERE.md)가
> 따로 맡는다. 이 폴더의 결과물을 **대체하지 않고 옆에 붙는다**(05의 D6).

대상 화면
- `src/app/admin/(shell)/content/education/regular-classes/[id]/page.tsx` — 추가·수정 **둘 다**
- 실제 폼 본체: `src/components/admin/content/education/regular-classes/regular-class-form.tsx`

> **`new/page.tsx`는 없어졌다** (2026-08-04, [03](../03-regular-class-form-merge/00-START-HERE.md) 완료).
> `/new`는 `[id]`에 `id === "new"`로 들어오고, 껍데기는
> [`ContentFormShell`](../../06-admin/06-콘텐츠-폼-공용셸.md)이 담당한다.
> 페이지는 얇은 껍데기이므로 이 작업의 코드 변경은 사실상
> `regular-class-form.tsx` 한 파일 + 데이터 층에서 끝난다. **페이지는 건드릴 일이 없다.**

## 읽는 순서

1. [01-요구사항.md](01-요구사항.md) — 무엇을 만드는가, 받아들임 기준
2. [02-현황분석.md](02-현황분석.md) — 지금 폼·스키마가 어떻게 생겼는가
3. [03-데이터모델.md](03-데이터모델.md) — 추가 컬럼과 마이그레이션 DDL **초안**
4. [04-구현계획.md](04-구현계획.md) — 단계별 작업 순서 **초안** (파일별 diff 지점)
5. [05-검증체크리스트.md](05-검증체크리스트.md) — 끝났다고 말하기 전 확인할 것
6. [06-교차검증-결과.md](06-교차검증-결과.md) — 위 문서를 코드·codex로 검증한 결과
7. [07-실행계획-확정.md](07-실행계획-확정.md) — **실제로 따라가는 계획**

`07`의 부속 문서 — 해당 단계에 가서 편다.

| 문서 | 07의 어디 |
|------|-----------|
| [08-HTML정제-설계.md](08-HTML정제-설계.md) | 3단계 — 파이프라인·HTML 정제 옵션 |
| [09-CSS스코프-설계.md](09-CSS스코프-설계.md) | 3단계 — CSS 스코프·컨테인먼트 (**리스크가 여기 몰려 있다**) |
| [10-마이그레이션-DDL.md](10-마이그레이션-DDL.md) | 1단계 — SQL 전문 |

> `03`·`04`는 코드 대조 전에 쓴 초안이라 **틀린 곳이 3군데 있다**(06 §1).
> DDL·단계·완료 조건이 어긋나면 **`07`이 우선**한다.

## 결정이 필요한 것 (착수 전 사용자 확인)

| # | 질문 | 기본안(답 없으면 이걸로 간다) |
|---|------|------------------------------|
| D1 | HTML 본문을 **DB 컬럼**에 넣을지, **Storage 파일**로 둘지 | DB 컬럼(`detail_html`). 이유는 [03-데이터모델.md](03-데이터모델.md) §3 |
| D2 | 업로드한 HTML을 **그대로 렌더**할지, 정제(sanitize) 후 렌더할지 | 정제 후 렌더. **저장 시 + 렌더 직전 2중** — 저장만으로는 안 닫힌다(06 §P1-3) |
| D3 | 원데이 클래스에 **시간(HH:MM)**까지 받을지 | 날짜만. 시간은 기존 `duration` 자유 텍스트로 적는다 |
| D4 | 기존 4개 과정(4주·6주 등)의 일정 유형 초기값 | `multi`(다회차), 날짜는 비움 |
| D5 | 업로드 HTML의 `<style>`·`class` 처리 | ✅ **`<style>` 유지 + CSS 스코프 구현.** `class`도 허용 → [08](08-HTML정제-설계.md) |
| D6 | 업로드 **원본(raw)** 저장 위치 | ✅ **관리자 전용 별도 테이블.** 같은 테이블이면 익명에게 공개된다(06 §P1-2) |
| D7 | 서버 액션에 `requireAdmin` 추가 | ✅ **별도 결정기록.** 기존 인증 미배선 문제라 여기 넣으면 범위가 번진다 |
| D8 | 마이그레이션 적용 시점 | ✅ **코드 먼저, DB 적용은 나중에.** SQL은 이번에 완성해 둔다 |
| D9 | 업로드 미리보기 | **v1 생략.** 샌드박스 iframe은 실제 결과와 다른 화면을 보여준다(06 §3) |

✅ = 2026-08-03 사용자 확정. D1~D4는 기본안 유지.

D1·D2·D5·D6은 보안과 직결된다. [01-요구사항.md](01-요구사항.md) §4와
[06-교차검증-결과.md](06-교차검증-결과.md) §2를 읽고 결정한다.

**D5는 권장안(둘 다 제거)이 아닌 쪽이다.** 대가는 [08](08-HTML정제-설계.md)에 적었다.
